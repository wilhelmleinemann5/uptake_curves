import { create } from 'storybook/theming';
import logoUrl from '../public/logo-maersk.svg';

export default create({
  base: 'light',
  brandTitle: 'Maersk Design System',
  brandUrl: '/',
  brandImage: logoUrl,
});
