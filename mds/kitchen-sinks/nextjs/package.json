{
    "name": "kitchen-sink-nextjs",
    "version": "1.0.0",
    "description": "App for testing community components in Next.js",
    "private": true,
    "main": "index.js",
    "scripts": {
        "dev": "npm run clean && nx run nextjs:map-node-modules && NODE_OPTIONS=--conditions=development next dev",
        "build-app": "npm run clean && nx run nextjs:map-node-modules && next build",
        "clean": "rm -rf node_modules .next",
        "preview": "next start",
        "e2e:headless:ci": "start-server-and-test preview http://localhost:3000 'cypress run --record'",
        "e2e:headless": "start-server-and-test dev http://localhost:3000 'cypress run'",
        "e2e:interactive": "start-server-and-test dev http://localhost:3000 'cypress open'"
    },
    "license": "MIT",
    "dependencies": {
        "@maersk-global/fonts": "^1.6.0",
        "@maersk-global/icons": "^1.23.0"
    }
}