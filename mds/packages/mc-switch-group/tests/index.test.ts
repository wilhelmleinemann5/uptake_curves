import { expect, test } from 'vitest';

// Use Vitest to test JS functions that you use in your component
// i.e. some helpers functions, api functions, services, utils, etc.
// For testing component itself (test rendering, passing props to component, clicking, all interactions, etc.)
// use Cypress component testing
test('Placeholder test case', () => {
  expect(true).toBeTruthy();
});
